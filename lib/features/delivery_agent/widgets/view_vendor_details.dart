import 'package:flutter/material.dart';

class ViewVendorDetails extends StatelessWidget {
  const ViewVendorDetails({super.key});

  @override
  Widget build(BuildContext context) {
    return Card(child: Text("Vendor details"));
  }
}
